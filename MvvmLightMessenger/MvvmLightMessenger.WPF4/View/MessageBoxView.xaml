<UserControl x:Class="MvvmLightMessenger.WPF4.View.MessageBoxView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           Visibility="{Binding DialogVisibility}"
           Style="{StaticResource DialogControlStyle}" >
    <Grid x:Name="LayoutRoot" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" MaxWidth="250"/>
    <Button x:Name="CancelButton" Content="取消" Command="{Binding CancleCommand}"  Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="1" />
    <Button x:Name="OKButton" Content="确定" Command="{Binding OKCommand}" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,79,0" Grid.Row="1" />
    </Grid>
</UserControl>